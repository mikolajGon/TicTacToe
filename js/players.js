class Player {
  constructor(name, symbol, active = true, isComputer = false) {
    this.name = name;
    this.symbol = symbol;
    this.active = active;
    this.isComputer = isComputer;
  }
}

